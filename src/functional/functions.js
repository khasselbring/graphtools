/**
 * @module Functional
 */


/**
 * Returns a list of calls to this lambda function with possibly different partials.
 * @param {Node} lambda A lambda node in the graph.
 * @param {Portgraph} graph the portgraph.
 * @returns {}
 */
export function calls (lambda, graph) {
  
}
